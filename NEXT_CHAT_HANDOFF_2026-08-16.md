# 装配赛正式代码交接文档（2026-08-16）

## 〇、2026-08-16后续最终确认（优先级最高）

本节是用户在本文件形成后的重新确认；如与下方第二、三、九、十一、十二节冲突，以本节为准。

- 固定并保存方块/托盘拍照点。
- 操作者先把吸盘移动到红色方块真实抓取中心、红色托盘真实放置中心，分别只读保存一个完整TCP六维位姿。
- 两个完整TCP自身的Z就是实际抓取/放置接触Z；旧`contact_z.pick/place`配置、界面表格、采集Worker和运行参数已删除。
- 保存红色基准后保持对应红色目标不动，机器人回到固定拍照点完成对应九点标定。
- 九点只建立像素变化到机器人XY修正量的映射；中心帧保存六色像素中心和角度。
- 批准新九点文件不再清除预先保存的红色抓取/放置基准；改变拍照点或活动TCP仍会使对应基准失效。
- 正式XY目标为`红色基准TCP_XY + 九点映射(当前本色中心像素 - 标定红色中心像素)`；高位使用拍照点安全Z，下降接触时直接使用对应红色基准TCP的Z。
- 当前代码已按此口径修改，并新增回归测试覆盖“九点批准保留基准”和“下降使用基准TCP自身Z”。
- “相机与视觉”页已新增Block/Tray各自的“拍照并用当前参数识别”诊断按钮：单次新帧、同帧六色、保存原图和标注图；有有效九点时返回物理偏移，无九点时返回像素结果；不运动、不写IO、不改配置、不批准参数。
- 比赛页发送的文字指令会同步显示到“语音盒子测试”页的最近控制文字框，并标记`[文字控制]`；真实ASR结果标记`[语音识别]`，两种来源不会混淆。
- “比赛任务”页已新增隔离的任务卡大模型测试：真实task_card新帧发送Qwen，显示原始响应、解析结果、正式协议结果、模型请求号和耗时；复用正式提示词/封装/校验，但不进入比赛会话、不连接机器人、不执行结果。

## 一、工程与启动入口

当前唯一正式工程：

```text
E:\iwen-codex\智能装配代码\装配赛正式代码
```

主要入口：

```text
启动正式比赛.cmd
启动MVS视觉服务.cmd
启动离线视觉工具.cmd
启动MVS实时画面.cmd
```

正式程序、视觉服务、离线工具和实时画面程序不能同时争抢同一台MVS相机。只查看实时画面时，应先关闭MVS视觉服务。

## 二、最高优先级：用户最后确认的视觉逻辑

以下内容是下一窗口必须遵守的最终口径；如与旧文档、旧代码或本文件后面的历史说明冲突，以本节为准。

### 1. 九点标定的作用

- 相机安装在机器人末端，机器人携带相机移动九个位置。
- 九点期间只跟踪同一个固定红色物块中心。
- 每个采样点记录该红块中心的像素坐标，以及机器人在该点产生的工具XY位移。
- 九点拟合结果只负责建立“像素变化量 → 机器人XY修正量”的关系。
- 用户不要求在九点完成后把吸盘中心移动到红块中心，也不要求额外示教红色物理锚点。

### 2. 六色视觉原点

- 在九点中心拍照位置，应识别红、橙、黄、绿、蓝、紫六个匹配框。
- 将六个匹配框各自的中心像素和角度保存为六个颜色各自的视觉原点。
- 下一次在同一拍照点拍照时，每种颜色只能与自己的标定视觉原点比较。
- 正式视觉应返回每种颜色相对自身原点的：
  - `delta_x_tool_m`
  - `delta_y_tool_m`
  - `delta_r_rad`
- 这三个值表达标定画面与当前画面之间的XY和角度变化。

### 3. 正式抓放应采用的原则

- 不新增“Block红色基准抓取TCP”或“Tray红色基准放置TCP”。
- 不要求操作者让吸盘中心对准红块或红色托盘。
- 正式运动应在程序原有的抓取/放置基准位姿上，叠加目标颜色自己的`ΔX、ΔY、Δ角度`。
- 方块和托盘分别使用自己的九点矩阵、六色视觉原点和当前检测结果。
- 放置方向若需要让方块与托盘匹配，可使用：

```text
旋转修正 = 托盘目标颜色 delta_r_rad - 方块目标颜色 delta_r_rad
```

### 4. 下一窗口仍需首先确认的一点

用户已经明确否定“吸盘对准红色目标并保存物理锚点”，但当前对话尚未最终指出“原有抓取/放置基准位姿”具体来自当前程序中的哪组数据。

下一窗口应先检查物理运动旧实现和现有配置，确认以下哪一种才是项目原本使用且符合用户意图的基准：

- 固定拍照点到目标的既有基准位姿；或
- 当前程序原来保存的其他抓取/放置位姿。

在确认之前，不要再次凭空新增物理点，也不要进行真实抓放。

## 三、当前代码中存在的最新错误实现

上一轮修改误把用户的视觉原点理解成“一个红色物理锚点带出六色位置”，因此当前工作区代码暂时包含以下错误实现：

- `config/real/motion.json`新增了：

```json
"reference_anchors": {
  "blocks": "UNSET",
  "trays": "UNSET"
}
```

- `app/ui.py`的“机器人现场配置”页新增了两个错误按钮：
  - 保存当前TCP为Block红色基准抓取点；
  - 保存当前TCP为Tray红色基准放置点。
- `app/point_capture_worker.py`新增了`RobotReferenceAnchorCaptureWorker`。
- `app/robot_config_editor.py`新增了物理锚点读写、清除和失效逻辑。
- `app/preflight.py`会因为两个物理锚点为`UNSET`而阻止运动。
- `app/assembly.py`当前构造函数要求`reference_anchors`，并错误地以红色物理锚点作为XY基准。
- `app/runtime.py`、`app/coordinator.py`和`app/direct_assembly_worker.py`当前会把`reference_anchors`传给`AssemblyExecutor`。

这些改动与用户最后确认不符。下一窗口应删除/撤销上述物理锚点方案，并改成“原有物理基准位姿 + 本色视觉Δ值”。

当前两个锚点保持`UNSET`，因此在错误实现被撤销前，程序不会误执行真实抓放。不要现场示教这两个点。

## 四、当前已经正确完成、应保留的视觉改动

以下部分符合用户要求，不应随物理锚点回退而删除：

### 1. 六色视觉原点写入九点文件

`app/nine_point.py`和`vision/real_mvs_service.py`已经支持：

- 九点中心采样帧提取六色检测；
- 六色必须完整覆盖红、橙、黄、绿、蓝、紫；
- 保存每色的`pixel_u`、`pixel_v`、`r_image_deg`和`confidence`；
- 保存六色参考原图和标注图路径；
- 缺少任一颜色时禁止启用新的九点候选。

现有两套正式九点文件已经从各自中心采样图片补入六色视觉原点：

```text
calibration/real/blocks/9point_blocks.json
calibration/real/trays/9point_trays.json
```

两套文件均有六个`reference_detections`，且`reference_image_path`和`reference_annotated_image_path`对应文件实际存在。

### 2. 当前本色与本色原点的差值

`vision/workspace_localizer.py`已经能对每个颜色返回：

```text
delta_x_tool_m
delta_y_tool_m
delta_r_rad
reference_pixel_u
reference_pixel_v
current_pixel_u
current_pixel_v
```

使用两套中心参考原图重新检测时，六色的`delta_x_tool_m`、`delta_y_tool_m`和`delta_r_rad`均为零，说明“本色对本色原点”的计算闭环正确。

需要修改的是`dx_tool_m/dy_tool_m`及装配层如何使用这些结果，不是删除本色Δ值。

### 3. 正式视觉协议

`app/protocols.py`已经要求正式成功检测返回完整数值字段，并检查角度范围及非有限值。协议扩展可以保留，但下一窗口应根据最终运动公式判断是否还需要对外保留`dx_tool_m/dy_tool_m`，以及它们准确代表什么，避免字段语义混乱。

## 五、颜色检测当前规则

正式blocks和trays检测已经与离线工具统一为宽松比赛规则：

- HSV分割；
- 高斯模糊；
- 3×3开运算；
- 5×5闭运算；
- 查找外轮廓；
- 过滤低于`min_area_px × 0.05`的极小碎片；
- 同色存在多个目标时按面积最大选择；
- 面积相同时以置信度打破平局；
- 面积上下限、方形度、低置信度和多候选只作为警告，不拒绝最大目标。

九点标定仍保持严格规则：目标必须是唯一、有效、近似方形候选；九点任一点失败则本次会话停止，不静默删除坏点。

正式和离线检测都会保存原图及带框标注图。绿色是最终选中，黄色是相近候选，红色是过滤候选，青色是ROI。

## 六、正式任务顺序与漏检处理

用户确认的比赛逻辑：

- 任务卡二先给出六组方块颜色和托盘颜色顺序。
- 执行时按照任务卡顺序逐组处理，不是先把六个方块全部识别完再统一抓取。
- 托盘可以在抓取前一次拍照并缓存当时识别出的六色结果。
- 当前组的方块颜色未识别到时，在原拍照位置重拍一次；仍找不到则跳过该组，不开启吸盘。
- 当前组所需托盘颜色未识别到时，该组应在抓取前跳过。
- 如果已经吸起方块后才发现对应放置目标不可用，必须保证安全释放/停止，不能带着物块继续未知运动。
- 通信、相机、标定、坐标、机器人、IO、执行队列和急停异常不是普通漏检，必须立即停止。

## 七、已经处理过的运行问题

本阶段此前处理或确认过：

- 九点拍照中的`NameError: name 'limits' is not defined`已经修正。
- 机器人指令队列`exec_id`非空的问题已经增加等待到位/队列处理，不能连续无等待地下发`moveLine`。
- Windows中文输出造成的`UnicodeDecodeError`已经在机器人输出读取处增加编码兼容处理。
- 工具吸盘使用`TOOL_IO[1]=True`长期使能，`TOOL_IO[0]=True/False`控制吸取/释放并回读。
- DO0为光圈，True开、False关；现场曾出现请求值与回读值相反，仍需按真实控制器接线复核，不能仅靠软件猜测反转。
- 语音盒子与总控当前通过HTTP交互；健康检查已能识别`arm_speech_service`和`m28-4-real`。

## 八、相机、HSV与临时/离线工具

- `启动MVS实时画面.cmd`只显示实时画面，不保存、不识别、不连接机器人。
- 手动Block/Tray拍照入口保存真实原图，后续离线分析HSV和面积。
- 离线视觉工具可以导入单图或递归导入文件夹，展示原图、掩膜、检测标注和六色明细。
- 离线候选参数不会自动覆盖正式`camera.json`，除非走正式参数保存流程。
- 用户此前要求临时检测程序显示视觉实际返回值，并额外显示一张在图上标注最终返回点的图片；当前工具已经显示候选框、中心和返回详情，但下一窗口若继续改UI，应先实际启动核对当前界面，不要仅凭旧截图判断。

## 九、当前配置和安全状态

- 正式机器人：`rob1`。
- 相机序列号和两套正式九点身份检查当前可通过。
- 正式、维护点位运动速度比例为40%。
- 九点九个采样点之间保持15%。
- 独立pick/place接触Z入口已删除；两个红色完整基准TCP各自包含真实接触Z，仍须现场示教确认。
- 当前`VERSION.json`仍是`hardware_validation_required`且`robot_motion_authorized=false`。
- 静态赛前检查还会因为版本状态以及误加的两个`reference_anchors`为`UNSET`而失败。
- 在物理锚点错误实现撤销、正确基准位姿确认及真实高位测试完成前，不允许宣称正式抓放已验收。

## 十、测试与完整性

在加入交接文档前，代码测试结果为：

```text
119 passed
```

当时`MANIFEST.sha256`已经重新生成并通过测试。新增本交接文档后，应再次运行：

```powershell
.\.runtime\Scripts\python.exe tools\generate_manifest.py
.\.runtime\Scripts\python.exe -m pytest -q
```

## 十一、下一窗口推荐执行顺序

1. 首先阅读本文件第二、三、四节。
2. 不要求用户重新九点，也不要让用户示教红色物理锚点。
3. 检查`app/assembly.py`在误改前使用的抓取/放置物理基准来源，向用户用一句明确公式确认。
4. 删除`reference_anchors`配置、UI按钮、Worker、赛前门控和各层参数传递。
5. 保留九点JSON中的六色视觉原点，以及`delta_x_tool_m/delta_y_tool_m/delta_r_rad`计算。
6. 将正式目标改为“确认后的原有基准位姿 + 当前本色ΔX/ΔY/ΔR”。
7. 更新相关测试，至少验证：参考图Δ值全零、目标移动后的Δ值方向正确、装配层确实叠加本色Δ而非红色锚点。
8. 更新README、真机手册和旧交接文档中刚加入的错误“红色物理锚点”说明。
9. 重新生成MANIFEST并运行全套测试。
10. 只做无IO高位单组验证；结果正确后再逐级进行下降、吸取、放置和完整六组。

## 十二、给下一窗口的首句提示

可以直接对新聊天说：

```text
请先阅读 E:\iwen-codex\智能装配代码\装配赛正式代码\NEXT_CHAT_HANDOFF_2026-08-16.md。
以第二节“用户最后确认的视觉逻辑”为最高优先级。当前代码误加了红色物理锚点，先不要运动，也不要让我重新标定；先检查原有抓取/放置基准位姿来自哪里，再撤销物理锚点方案并保留六色本色Δ值。
```

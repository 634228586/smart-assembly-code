# 智能装配正式代码：跨电脑部署交接文档

更新时间：2026-08-17  
适用目录：本项目根目录（下文记为 `<项目根目录>`）

这份文档用于把项目迁移到另一台 Windows 电脑。默认场景是“更换电脑，但继续使用同一套 AUBO 机器人、MVS 相机和 AI 盒子”。如果同时更换机器人、相机、相机安装位置、拍照点或 TCP，请按“更换硬件时的强制处理”执行，不能直接沿用旧标定。

## 1. 先判断属于哪种迁移

### 1.1 只更换电脑，硬件不变

可以保留以下正式数据：

- `config/real/` 下的现场配置；
- `calibration/real/` 下的已批准九点文件；
- `data/sessions/` 和 `logs/`（用于追溯；若要继续使用现有九点文件，必须一并保留其引用的采样原图和标注图）；
- 项目源码、启动脚本和 `requirements-lock.txt`。

必须在新电脑重新完成：运行环境、环境变量、网络/防火墙、MVS 机器级驱动检查、只读身份检查和赛前检查。新电脑不能因为旧电脑运行过就跳过这些检查。

### 1.2 更换机器人、相机或安装关系

必须把它视为新设备：重新填写机器人身份、相机序列号、真实点位、红色抓取/放置完整 TCP，重新采集 blocks/trays 两套九点并批准。旧九点文件绑定旧机器人、旧相机、旧拍照点和旧 TCP，不能复制后继续用于新硬件。

## 2. 复制项目

建议复制整个项目目录，保持目录结构不变。例如：

```text
<项目根目录>\
  app\  config\  calibration\  vision\  voice\  tools\  tests\
  启动正式比赛.cmd
  启动MVS视觉服务.cmd
  requirements-lock.txt
```

不要只复制 `app` 目录。配置、标定、相机服务、启动脚本和 `data/sessions` 都是运行链路的一部分。

新电脑上的项目根目录可以不同于旧电脑；程序会根据自身脚本位置解析路径，不要求继续使用旧电脑的盘符。配置和证据中出现的旧绝对路径如果仍存在，迁移后应优先检查并改成项目内相对路径。

## 3. 新电脑的软件前置条件

在 Windows PowerShell 中确认：

1. Windows 版本满足现场要求，系统时间和时区正确。
2. 安装 **Python 3.11 x64**，并确认 `py -3.11` 可用。
3. 安装 AUBO 官方 SDK/控制器配套组件，并安装真实 MVS 相机的机器级 MVS Runtime、驱动和 VC++ Runtime。
4. 确认 MVS Runtime 的位数与 Python/系统一致，能枚举真实相机。
5. 确认新电脑的有线网卡与机器人、AI 盒子处于同一可通信网络；不要让电脑 IP 与设备 IP 冲突。
6. 防火墙允许本机程序监听 `127.0.0.1:9001`（视觉服务）和 `127.0.0.1:8766`（语音到 Qt 的本地命令端口），并允许向机器人、AI 盒子和 HTTPS 出站。

## 4. 构建包内 Python 运行环境

在项目根目录打开 PowerShell，执行：

```powershell
cd <项目根目录>
.\tools\构建运行环境.cmd
```

脚本会使用 `requirements-lock.txt` 创建 `.runtime`，并安装固定版本的 PySide6、OpenCV、numpy、PyYAML、OpenAI 客户端、AUBO SDK 等依赖。完成后应看到导入检查通过。

如果项目是从旧电脑完整复制过来的、且 `.runtime` 已存在，可直接使用它；若要重建，先人工备份旧 `.runtime`，再删除后重新执行构建脚本。构建脚本为了避免覆盖比赛环境，不会自动删除已有 `.runtime`。

验证运行时：

```powershell
.\.runtime\Scripts\python.exe -c "import PySide6,cv2,numpy,yaml,openai,httpx,pyaubo_sdk; print('runtime ok')"
```

## 5. 设置机密环境变量

密码和 API Key 不写入项目 JSON、Python、交接文档或日志。用 PowerShell 写入当前 Windows 用户环境变量；设置后需重新打开终端：

```powershell
[Environment]::SetEnvironmentVariable('AUBO_RPC_PASSWORD','<机器人密码>','User')
[Environment]::SetEnvironmentVariable('DASHSCOPE_API_KEY','<DashScope API Key>','User')
[Environment]::SetEnvironmentVariable('DASHSCOPE_BASE_URL','<DashScope Base URL>','User')
[Environment]::SetEnvironmentVariable('DASHSCOPE_MODEL','<实际模型名>','User')
```

检查变量是否存在时只检查是否为空，不要把值打印出来：

```powershell
foreach ($n in 'AUBO_RPC_PASSWORD','DASHSCOPE_API_KEY','DASHSCOPE_BASE_URL','DASHSCOPE_MODEL') {
  $v = [Environment]::GetEnvironmentVariable($n,'User')
  "$n : " + (-not [string]::IsNullOrWhiteSpace($v))
}
```

正式启动脚本会把用户变量带入当前进程。新电脑若使用不同用户登录，必须在实际运行比赛的那个 Windows 用户下重新设置。

## 6. 需要核对的配置文件

默认现场值如下。若硬件不变，先不要随意改动；若硬件或网络改变，按实际设备修改并重新验收。

| 文件 | 关键字段 | 当前现场值/要求 |
|---|---|---|
| `config/real/endpoints.json` | `robot_rpc` | AUBO `192.168.34.10:30004` |
|  | `vision_service` | 本机 `127.0.0.1:9001` |
|  | `qt_command_listener` | 本机监听 `127.0.0.1:8766` |
|  | `speech_service` | AI 盒子 `192.168.34.200:8765` |
|  | `qwen_api` | 使用 `DASHSCOPE_*` 环境变量 |
| `config/real/robot.json` | 厂家/型号/名称 | `AUBO` / `AUBO-i5H` / `rob1` |
|  | `binding_id` | `AUBO-i5H\|192.168.34.10\|rob1`，必须与真实控制器一致 |
|  | `active_tcp` | `flange_zero_tcp`，六维 offset 全零 |
| `config/real/motion.json` | 固定点位 | 真实示教的待机、任务卡、blocks、trays 六轴弧度 |
|  | `reference_anchors` | Block 红色抓取完整 TCP、Tray 红色放置完整 TCP，包含真实接触 Z |
|  | 正式/维护速度 | 实际速度比例 **70%**，批准上限 **70%** |
|  | 九点速度 | 九个采样点之间固定 **50%**；中心进出使用 **70%** |
|  | 物理速度 | 关节 `0.15 rad/s`、直线 `0.03 m/s`；加速度分别 `0.30 rad/s²`、`0.10 m/s²` |
| `config/real/camera.json` | `serial_number` | 当前相机 `DA3968689`；必须从新电脑实际枚举结果核对 |
|  | 三套 profile | `task_card`、`blocks`、`trays` 的曝光、增益、白平衡、ROI、软件触发参数 |
|  | blocks/trays detector | 现场实拍后离线确认 HSV、ROI、面积和置信度，不要凭旧相机经验填写 |
| `config/real/competition.json` | 任务/语音规则 | 两张任务卡；首次唤醒词为“小具同学”，后续按现场确认规则执行 |
| `config/real/suction_io.json` | 吸盘/光圈 IO | `TOOL_IO[1]` 长期开启；`TOOL_IO[0]` 为吸取/释放；标准 `DO0` 控制光圈 |

### 6.1 只换电脑时的配置检查

打开项目根目录中的 JSON 文件，重点确认：

- `robot.json` 的 `binding_id` 与真实机器人完全一致；
- `camera.json` 的序列号与 MVS 枚举结果一致；
- `motion.json` 的四个固定点和两套 reference anchor 没有被旧版本覆盖；
- `motion.json` 中正式/维护 `speed_fraction` 和 `maximum_authorized_speed_fraction` 都是 `0.7`；九点网格是 `0.5`；
- `calibration/real/blocks/9point_blocks.json` 和 `calibration/real/trays/9point_trays.json` 的机器人绑定、相机序列号、TCP、拍照点与当前配置一致。

## 7. 启动顺序

### 7.1 先启动真实 MVS 视觉服务

在一个 PowerShell 窗口运行：

```powershell
.\启动MVS视觉服务.cmd
```

保持窗口打开。服务会按 `camera.json` 的序列号打开唯一相机，不会在枚举到多台相机时随意选择其他设备。服务启动后可在另一个 PowerShell 做只读健康检查：

```powershell
Invoke-RestMethod http://127.0.0.1:9001/health
```

必须核对服务身份是 `real_mvs_vision`、状态 ready、相机序列号正确。若服务无法打开相机，先处理 MVS Runtime、USB/GigE 网卡、相机供电和序列号，不要直接启动正式比赛。

### 7.2 再启动正式总控

另开 PowerShell 运行：

```powershell
.\启动正式比赛.cmd
```

总控不会自动启动 MVS 服务。若缺少 `.runtime`，先回到第 4 节构建环境。正式总控会设置 Qt 软件渲染参数，适合现场电脑没有可用硬件 OpenGL 的情况。

### 7.3 可选工具

- `启动MVS实时画面.cmd`：只查看相机实时画面，便于确认取流和构图。
- `启动离线视觉工具.cmd`：导入现场图片，复核 blocks/trays 的 HSV、面积、ROI 和检测结果；不会连接机器人。

## 8. 新电脑上的验证顺序

### HSV 复查图片选择规则

如需重新审查或修改 HSV，分别在 Block 和 Tray 场景中使用“拍照并用当前参数识别”或“拍照取图”产生的图片，按时间分别选择最新的一张 Block 原图和一张 Tray 原图。使用原始 PNG，不使用 `*-annotated.png`，也不用九点 `point-N.png` 或任务卡图片代替工作区图片。

用户明确要求开始 HSV 调整后，离线计算出的最终值必须直接写入正式 `config/real/camera.json` 的 `profiles.blocks.detector.hsv_ranges` 和 `profiles.trays.detector.hsv_ranges`，不能只停留在内存预览或 `data/offline_vision_candidates` 候选文件中。写入后使用相同两张原图离线复验、更新 `MANIFEST.sha256` 并运行测试；真实程序下次启动前重启 MVS 视觉服务，使新配置被正式加载。除非用户另行要求，不修改曝光、增益、白平衡、ROI、面积或置信度。

按以下顺序逐项记录结果，不要跳到真实比赛：

1. **软件验证**：运行 `tools\run_tests.ps1`，或执行 ` .\.runtime\Scripts\python.exe -m pytest -q`；当前代码基线应为 126 项通过。
2. **静态配置验证**：执行：

   ```powershell
   .\.runtime\Scripts\python.exe tools\preflight_report.py
   ```

   输出中的红项逐条处理；不要用口令绕过真实设备、相机、九点或路径安全检查。

3. **MVS 验证**：启动视觉服务，确认健康状态、序列号和服务身份；在机器人处于对应拍照点时分别做 Block/Tray 新帧取图。
4. **离线视觉验证**：用新电脑采集的图片复核 blocks/trays detector。更换相机、镜头、分辨率或安装位置时必须重新计算参数。
5. **机器人只读验证**：确认唯一机器人 `rob1`、RobotMode=`Running`、SafetyMode=`Normal`、执行队列为空、TCP offset 六项全零；确认急停和围栏正常。
6. **点位验证**：操作者在机器人现场确认四个固定点以及两个完整红色抓放 TCP。点位移动按钮只在人工确认整条直达路径无人、无障碍物后使用。
7. **九点验证**：如果机器人、相机、拍照点或 TCP 没有变化，可核对已批准文件绑定关系；只要其中任意一项变化，就重新完成 blocks/trays 两套九点。九点要求最大单点误差 ≤0.5 mm、RMS ≤0.3 mm，并通过 +X、+Y、0°、+10° 四项方向验证。
8. **语音/模型验证**：核对 AI 盒子 `/health`、真实 ASR/TTS；确认 DashScope 网络、模型和 Key 可用。任务卡大模型测试会产生外部 API 请求，现场确认后再执行。
9. **分级运动验收**：先单点，再下降，再 IO，再单组，最后才做完整流程。任何停止、急停、断线、重启或配置改变后，当前授权会失效，应重新做检查。

## 9. 现场安全约束

- 程序没有碰撞规划。每次正式授权前，操作者必须确认当前姿态到任务卡、blocks、trays 的直接路径无人、无障碍物。
- 当前正式和维护实际速度为 70%，九点采样点之间为 50%。70% 是现场批准上限，不代表可以跳过低速观察、围栏、急停和分级验收。
- 任何真实机器人、相机或 IO 操作前，都要明确连接对象、读取内容或即将执行的动作。
- 关闭窗口不是急停。异常时使用现场急停，再处理程序状态和控制器状态。
- 不要把密码、API Key、机器人登录信息写入 JSON、截图、日志或新的交接文本。

## 10. 常见问题排查

| 现象 | 优先检查 |
|---|---|
| 找不到 `.runtime` | 运行 `tools\构建运行环境.cmd`；确认 Python 3.11 x64 和网络可用 |
| MVS 服务无法打开相机 | MVS Runtime/VC++、相机供电、网卡/USB、序列号、`camera.json` |
| 健康检查不是 ready | 服务窗口错误、端口 9001 冲突、MVS 设备被其他程序占用 |
| 机器人身份不匹配 | `endpoints.json`、`robot.json` 的 IP、型号、名称、binding_id；不要强行改成“看起来相近”的值 |
| 语音无响应 | AI 盒子 IP/8765、`/health`、ASR/TTS 后端、网络、防火墙 |
| 任务卡识别失败 | 相机是否在任务卡点、是否为新帧、DashScope 三个环境变量、模型返回是否符合协议 |
| 九点文件被判定不可用 | 文件绑定的机器人/相机/TCP/拍照点与当前配置不一致；按第 8 节重新标定 |
| 速度配置报越限 | 正式/维护速度必须满足 `0 < speed_fraction <= maximum_authorized_speed_fraction <= 1`；当前应为 `0.7 <= 0.7`，九点网格为 `0.5` |
| 启动器双击后立即退出 | 从 PowerShell 启动并保留错误信息；先检查 `.runtime`、环境变量和当前工作目录 |

## 11. 交接完成标准

只有满足以下条件，才可把新电脑交给比赛操作者：

- [ ] 项目目录完整，启动脚本可执行。
- [ ] `.runtime` 导入检查通过，`pytest` 全部通过。
- [ ] MVS Runtime 能打开指定相机，视觉服务健康检查通过。
- [ ] 四个固定点、两个红色抓放完整 TCP 和当前速度配置已由现场人员核对。
- [ ] `robot.json`、`camera.json`、`motion.json` 与真实设备身份一致。
- [ ] 若硬件未变，两套九点文件的绑定关系已核对；若硬件变更，两套九点已重新采集并批准。
- [ ] AI 盒子 ASR/TTS、DashScope 和语音到 Qt 的链路已验证。
- [ ] 赛前检查没有未处理的真实安全红项，且操作者知道急停、停止和恢复流程。

相关资料：

- [比赛启动手册](docs/比赛启动手册.md)
- [配置字段说明](docs/配置字段说明.md)
- [真机示教与标定手册](docs/真机示教与标定手册.md)
- [赛前检查清单](docs/赛前检查清单.md)
- [故障与急停恢复](docs/故障与急停恢复.md)

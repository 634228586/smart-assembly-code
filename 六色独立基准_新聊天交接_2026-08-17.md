# 六色独立基准与真机修正——新聊天交接文档

更新时间：2026-08-18  
项目目录：`E:\iwen-codex\智能装配代码\装配赛正式代码`

## 1. 当前结论

六色独立基准已经实现，不再是待开发状态：

- Block：红、橙、黄、绿、蓝、紫各1个抓取TCP，共6个。
- Tray：红、橙、黄、绿、蓝、紫各1个放置TCP，共6个。
- 总计12个独立TCP，当前全部已保存为有效6D数值，没有 `UNSET`。
- Block和Tray仍各使用1套九点标定，共2套，不是每个颜色单独标定。
- 运行时使用“本颜色当前检测相对本颜色参考图的变化”修正该颜色的独立示教TCP。
- 九点标定完成/重新批准不会再自动清空12个示教点。
- 当前不需要重新九点标定，也不需要重新示教12个点。

## 2. 接手后的第一件事

必须关闭并重新启动正式比赛程序，然后才能继续真机测试。

原因：当前检测到的比赛程序进程启动于 `2026-08-17 20:11:47`，但吸盘旋转方向修复写入 `app/assembly.py` 的时间是 `20:13:55`。当前进程仍加载旧旋转逻辑。

重启方式：关闭当前比赛界面，再运行项目中的“启动正式比赛”。不要在旧进程中继续抓放。

重新启动后：

1. 不重新标定。
2. 不重新示教。
3. 先只做一个颜色的小范围单组抓放。
4. 观察日志中的视觉变化、基准XY、最终XY和末端补偿角。
5. 确认平移和旋转方向都正确后，再扩大到六色完整流程。

## 3. 当前正式配置

- 机械臂：`rob1`
- 机械臂地址：`192.168.34.10:30004`
- 正式配置目录：`config/real`
- 正式运动配置：`config/real/motion.json`
- Block正式标定：`calibration/real/blocks/9point_blocks.json`
- Tray正式标定：`calibration/real/trays/9point_trays.json`
- 正式运行速度比例：70%
- 批准最高速度比例：70%
- 九点网格内移动速度比例：50%（已由15%改为50%）
- Block九点步长：20 mm × 20 mm
- Tray九点步长：20 mm × 20 mm

## 4. 当前两套九点标定

### Block

- calibration_id：`N9_BLOCKS_DA1C04549504`
- RMS：`0.045350691111932 mm`
- 最大误差：`0.0711628818568092 mm`
- 六色参考图：`data/sessions/nine-blocks-20260817-193929-220267/calibration/blocks/six-color-reference.png`

### Tray

- calibration_id：`N9_TRAYS_EAD8AE27A76F`
- RMS：`0.0564267378048127 mm`
- 最大误差：`0.0999298507856669 mm`
- 六色参考图：`data/sessions/nine-trays-20260817-194245-141025/calibration/trays/six-color-reference.png`

两套文件的 `approval_source` 仍为 `operator_skipped_direction_validation`，但当前现场问题不是标定拟合误差。不要仅凭这个字段要求重新标定。

## 5. 当前12个独立示教TCP

数据来自当前 `config/real/motion.json`：

```json
{
  "blocks": {
    "红": [0.42586606256243514, 0.21106971302472866, 0.16531758472502028, -3.1414278790594183, -0.000009441019321554954, 1.570829132508995],
    "橙": [0.4252100109314624, 0.27854211006264307, 0.1663312802269465, -3.1414813782520405, 0.0000791798507627225, 1.5708705696507363],
    "黄": [0.4859197771112311, 0.21106318387998263, 0.1725171336640739, -3.1414124715317717, 0.000029486626504651636, 1.570823499268001],
    "绿": [0.3483184824453127, 0.21107805358344475, 0.17327986766516598, -3.1415243009438, -0.000014800639384949109, 1.570796887579484],
    "蓝": [0.48708210060156243, 0.27854732199301413, 0.16703289436719637, -3.1415276792910194, 0.000052228680727937095, 1.570872647420757],
    "紫": [0.36132110171502596, 0.2785329675914535, 0.17354592942588204, -3.1415444545484603, 0.000021333128487520937, 1.5708181105089367]
  },
  "trays": {
    "红": [0.4865800646493944, -0.19607207241885785, 0.1813427702853973, -3.140234393779083, -0.002686428305568489, 1.5709195652067123],
    "橙": [0.4875686777352293, -0.2662420205071698, 0.17435852390104944, 3.139967092916519, -0.0023365205045904015, 1.5710334608359948],
    "黄": [0.40563639276776864, -0.1960866258166962, 0.17819340011211365, 3.1398722484707853, -0.0021663169995901777, 1.5709951811425362],
    "绿": [0.406906262331572, -0.2662552929636027, 0.18161933015096568, 3.1399250816806776, -0.002339748937456082, 1.5710239272827307],
    "蓝": [0.31972543847533635, -0.1960720404390087, 0.17484412664073923, 3.1399157362382026, -0.0022924509921497024, 1.5710582981527355],
    "紫": [0.3197280719062366, -0.26626203992860614, 0.183158076808007, 3.139896476127328, -0.0023247358129049917, 1.5710539766125753]
  }
}
```

不要用旧聊天或旧文档中的红色TCP覆盖这些数值。

## 6. 已完成的UI与保存逻辑

现场配置页已经提供12个独立按钮：

- 6个“读取并保存当前TCP为 Block/颜色抓取基准”。
- 6个“读取并保存当前TCP为 Tray/颜色放置基准”。

当前行为：

- 单击按钮即读取当前TCP并真实写入配置，不再弹出需要选择“是”的二次确认框。
- 按钮只读当前TCP并保存，不会自动运动。
- UI显示每个颜色当前保存的完整TCP或 `未设置`。
- 保存后会更新正式配置；比赛中指定颜色缺点时，程序会在任何运动和吸盘动作前拒绝运行。

### 示教点持久性

此前旧逻辑在重新九点或批准九点后会清空场景六色基准，造成用户已经保存的点消失。该行为已经取消：

- 重新九点、完成九点、批准九点：不再删除12个示教点。
- 修改拍照点或更换活动TCP：仍可能使相关基准/标定失效，这是必要的硬件坐标安全策略。
- 不要恢复“九点后自动清空示教点”的旧逻辑。

早期因旧清空逻辑丢失的数据未能可靠找回；上面列出的当前12个点是用户后来重新示教并已写入的正式数据。

## 7. 视觉字段与正确运行公式

`vision/workspace_localizer.py`仍会输出：

- `dx_tool_m`、`dy_tool_m`：相对红色参考中心的旧兼容字段。
- `delta_x_tool_m`、`delta_y_tool_m`：当前目标相对本颜色参考中心的变化。
- `delta_r_rad`：当前目标相对本颜色参考角度的变化。

六色独立运行必须使用 `delta_x_tool_m`、`delta_y_tool_m`、`delta_r_rad`，不能使用相对红色的 `dx_tool_m`、`dy_tool_m`做跨颜色长距离移动。

### 7.1 已发现并修复的XY错误

错误版本曾执行：

```text
poseTrans(本色示教TCP, [delta_x, delta_y, 0, 0, 0, 0])
```

这是错误的。九点输出在本套眼在手标定流程中已经包含相机运动和反向关系，再当作工具坐标送入 `poseTrans` 会发生二次转换，导致XY轴交换和方向错误。

修复依据：用六个参考图像素中心和六个独立示教TCP做交叉验证，发现视觉量与真实机器人基座XY变化近似等大反向。真机SDK数学接口也证明错误版本会把绿色块的视觉变化再次旋转到错误方向。

当前正确平移公式位于 `app/assembly.py` 的 `_apply_calibrated_xy`：

```text
最终基座X = 本色示教基准X - delta_x_tool_m
最终基座Y = 本色示教基准Y - delta_y_tool_m
```

Block：

```text
block_reference = 本色Block示教TCP（Z提高到拍照安全高度）
block_target.XY = block_reference.XY - 本色视觉delta.XY
下降Z = 本色Block示教TCP保存的Z
```

Tray：

```text
tray_reference = 本色Tray示教TCP（Z提高到拍照安全高度）
tray_xy_target.XY = tray_reference.XY - 本色视觉delta.XY
下降Z = 本色Tray示教TCP保存的Z
```

`poseTrans`现在不再承担视觉XY平移，只用于托盘末端旋转。

### 7.2 已发现并修复的旋转错误

错误版本：

```text
delta_rz = normalize_square_angle(tray.delta_r_rad - block.delta_r_rad)
```

现场发现吸盘旋转方向相反。当前相机图像角正方向与AUBO末端工具Z轴正旋转方向相反，因此已改为：

```text
delta_rz = normalize_square_angle(block.delta_r_rad - tray.delta_r_rad)
```

然后执行：

```text
tray_target = poseTrans(tray_xy_target, [0, 0, 0, 0, 0, delta_rz])
```

运行日志已经增加：

- 方块基准XY和最终XY。
- 托盘基准XY和最终XY。
- 末端补偿角（度）。

新聊天不要再次根据旧文档把旋转公式改回去。该修复尚需在重启后的真机单组流程中最终确认。

## 8. 两次错误的原因说明

### XY错误原因

开发时过早相信字段名 `delta_x_tool_m / delta_y_tool_m`，把它理解成仍需通过 `poseTrans`转换的工具坐标，没有先用六个真实示教TCP和六色参考像素核对坐标契约。原假机器人测试把 `poseTrans`简化成逐项相加，也没有模拟AUBO真实坐标旋转，因此未发现二次变换。

### 旋转错误原因

开发时直接采用数学上的“托盘角减方块角”，没有先验证当前眼在手安装下图像角正方向与机器人工具Z旋转正方向是否一致。当前安装存在方向反射，所以末端补偿必须整体取反。

九点RMS很小只能证明像素到标定数值拟合良好，不能单独证明这些数值在运行时应作为基座坐标还是工具坐标使用，也不能证明旋转正负号。

## 9. 安全指纹与曾出现的中途停止

正式运行会记录运行时指纹。该指纹实际包含：

- `app`、`vision`、`voice`中的程序代码；
- `config/real`配置；
- `calibration/real`标定。

只要比赛会话运行期间其中任何一项发生变化，下一条硬件命令都会被阻止。

曾出现错误：

```text
config/real 在比赛会话中发生改变，已阻止下一条硬件命令。
```

当时实际原因是比赛运行期间修改了 `app/assembly.py`，并非用户的示教点再次丢失。当前错误文案只提 `config/real`，描述不完整。

操作规则：完成所有代码/配置/标定修改后再重启比赛程序；正式运行开始后不要继续编辑这些文件。

## 10. Qwen任务卡识别临时失败

曾出现：

```text
识别失败：Qwen调用或结果解析失败：APIConnectionError: Connection error.
```

只读检查结果：

- `DASHSCOPE_API_KEY`环境变量存在。
- `DASHSCOPE_BASE_URL`已配置。
- 域名DNS正常。
- 443端口可连接。
- 不带密钥的HTTPS请求能到达服务并返回预期401，说明检查时网络链路已恢复。

因此该次是瞬时网络或服务连接抖动，不是相机、机器人、九点标定或任务卡图像失败。重新识别通常即可。后续如频繁发生，再考虑仅针对连接类错误增加有限重试；不要把任务卡识别失败误归因到装配坐标。

语音日志中类似：

```text
[instruction_ignored] 未命中任务卡触发规则：……
```

是ASR误听且未命中触发词，与Qwen任务卡识别是两条独立链路。

## 11. 测试与完整性状态

最后一次完整自动测试：

```text
132 passed in 3.73s
```

已覆盖并更新的关键测试包括：

- 六色独立基准选择。
- 缺少指定颜色基准时禁止任何运动和吸盘动作。
- 使用同色 `delta_x/delta_y`，不使用相对红色的旧位移。
- 视觉XY直接反向应用到基座坐标，不再通过 `poseTrans`平移。
- 托盘旋转补偿方向为 `block - tray`。
- 一键保存和九点后保留基准相关行为。

`MANIFEST.sha256`已在最后修改后重新生成并通过完整测试验证。

## 12. 重启后的真机验收顺序

必须逐级进行：

1. 关闭旧比赛程序并重新启动。
2. 只读确认机器人是 `rob1`、相机身份正确、两套calibration_id与本文一致。
3. 不移动相机、机器人底座、工作台或拍照点。
4. 选择一个色块和对应托盘，只小幅移动目标，优先控制在约10～20 mm范围。
5. 执行单组抓放，先观察高位XY方向是否朝目标移动。
6. 核对日志中的：本色视觉ΔX/ΔY/ΔR、基准XY、最终XY、末端补偿角。
7. 确认吸盘旋转方向后才允许下降和释放。
8. 单色通过后逐一验证六色，最后再运行完整比赛流程。

如果现场方向仍不对，先保留当次完整日志和新拍图，不要立刻重新九点或覆盖12个示教点。

## 13. 什么时候才需要重新标定或重新示教

当前不需要重新标定。只有以下硬件几何关系变化时才应重新九点：

- 相机或相机支架移动；
- 机器人底座或工作台移动；
- 活动TCP/吸盘工具发生变化；
- Block或Tray拍照关节点被更改；
- 图像分辨率或影响几何映射的相机参数改变。

当前不需要重新示教12个点。只有以下情况才重教相关点：

- 吸盘工具或TCP变化；
- 实际抓取/放置高度变化；
- 工作区物理布局整体重装；
- 用户明确确认某个已保存点本身不在目标中心。

不要为了修复运行公式而重教点，也不要用重标定掩盖坐标代码错误。

## 14. 关键文件

- `app/assembly.py`：六色基准选择、基座XY反向补偿、托盘旋转、运行日志。
- `vision/workspace_localizer.py`：同色参考变化字段计算。
- `app/robot_config_editor.py`：12点配置保存和校验。
- `app/point_capture_worker.py`：读取当前TCP并保存。
- `app/ui.py`：12个一键保存按钮及显示。
- `app/calibration_worker.py`：九点运动，当前网格速度50%。
- `app/integrity.py`：代码/配置/标定运行时指纹。
- `config/real/motion.json`：12个正式TCP及运动参数。
- `calibration/real/blocks/9point_blocks.json`：Block正式九点。
- `calibration/real/trays/9point_trays.json`：Tray正式九点。
- `tests/test_assembly_executor.py`：XY和旋转最终公式测试。

## 15. 新聊天可直接使用的启动指令

> 请完整读取 `E:\iwen-codex\智能装配代码\装配赛正式代码\六色独立基准_新聊天交接_2026-08-17.md`。六色独立12个TCP、九点50%速度、九点后不清空示教点、基座XY反向补偿以及吸盘旋转反向修复均已完成，完整测试132项通过。当前旧比赛进程早于最后旋转修复，先提醒我关闭并重新启动正式比赛程序；不要重新九点标定、不要重新示教、不要先改配置。重启后协助我用一个颜色做小范围单组真机验证，重点核对日志中的基准XY、最终XY和末端补偿角。未经我明确要求，不要控制机器人运动。

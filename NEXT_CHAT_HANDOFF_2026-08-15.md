# 项目交接摘要（2026-08-15）

## 2026-08-16最新坐标逻辑

- 九点九帧使用同一个固定红色目标拟合像素变化与机器人工具XY变化。
- 九点中心帧必须识别并保存六色中心像素和角度，作为六个视觉原点。
- 正式抓放使用现场示教的Block红色基准抓取TCP和Tray红色基准放置TCP，不再使用拍照TCP直接叠加视觉值。
- 当前两个物理基准在`config/real/motion.json`中都是`UNSET`，真实抓放会被阻止。现场分别将吸盘准确对准标定中心帧中的红色目标后，在“机器人现场配置”点击两个只读保存按钮。
- 当前目标XY=`红色物理基准XY + 九点映射(当前本色中心 - 标定红色中心)`；旋转修正=`托盘本色ΔR - 方块本色ΔR`。
- 拍照点、TCP或对应九点重新批准后，旧物理基准自动作废。

## 一、工程位置

当前正式工程：

```text
E:\iwen-codex\智能装配代码\装配赛正式代码
```

原始参考工程：

```text
E:\iwen-codex\装配赛正式代码\装配赛正式代码
```

原始交接文档：

```text
E:\xwechat_files\wxid_omfik71fw4cs22_bfa6\msg\file\2026-08\今日项目交接文档_2026-08-14.docx
```

## 二、运行环境处理

已经完成：

- 包内 Python Runtime 已改为当前电脑环境。
- 当前 Python 为3.11.9。
- MVS客户端为5.0.2。
- MVS SDK Runtime为4.8.1.2。
- 包内 MVS Python Wrapper与当前安装SDK示例匹配。
- 已处理旧电脑`C:\Users\dqq\...`路径依赖。
- 新生成的九点图片证据使用工程相对路径。
- 历史旧电脑绝对路径可按当前工程目录兼容解析。

没有修改真实机械臂点位、相机序列号、曝光、白平衡、接触Z或IO参数。

## 三、启动入口

正式程序：

```text
启动正式比赛.cmd
```

MVS服务：

```text
启动MVS视觉服务.cmd
```

离线视觉工具：

```text
启动离线视觉工具.cmd
```

## 四、运动速度

当前要求：

- 正式比赛运动：40%。
- 维护“移动到此点”：40%。
- 九点标定进入拍照中心：40%。
- 九点结束返回拍照中心：40%。
- 只有九个采样点之间的移动保持15%。

底层参数：

```text
关节速度：0.15 rad/s
关节加速度：0.30 rad/s²
直线速度：0.03 m/s
直线加速度：0.10 m/s²
```

MoveJoint和MoveLine每次运动前都会明确下发速度比例，避免沿用上一条运动的残留倍率。

## 五、九点标定

已经完成的逻辑：

- blocks和trays分别生成独立九点文件。
- 每次重新生成都从第1点开始。
- 不混用旧会话采样。
- 新标定完成前保留旧正式文件。
- 新图片证据保存为工程相对路径。
- JSON九点文件是正式程序使用格式。
- XML可能是相机SDK或历史标定文件，不能自动代替当前九点JSON。

九点标定仍保持严格识别：

- 面积检查。
- 方形检查。
- 最低置信度。
- 多候选拒绝。
- 必须找到唯一有效目标。
- 任意一点失败，当前九点会话中止。
- 下次从第1点重新开始。

当前两套真实九点仍未完成现场正式验收，因此真实比赛授权保持关闭。

## 六、HSV和颜色识别流程

主程序中已经隐藏以下旧操作区：

- HSV JSON手工编辑。
- 面积手工编辑。
- 自动填写面积。
- “真实六色同帧验证并批准”。

当前工作方式：

1. 使用Block/Tray拍照取图。
2. 保存现场原图。
3. 使用离线工具批量分析。
4. 计算或调整HSV。
5. 生成候选配置。
6. 人工确认后再更新正式`camera.json`。
7. 重启MVS服务。

赛前检查只检查颜色参数是否完整，不再依赖`detector.approved`。

## 七、离线视觉工具

已经建立独立工具，主要代码：

```text
app\offline_vision_tool.py
vision\offline_analyzer.py
```

当前功能：

- 添加多张图片。
- 递归添加整个文件夹。
- 选择blocks或trays。
- 读取当前正式`camera.json`参数。
- 查看整张原图。
- 查看六色HSV掩膜。
- 查看检测标注结果。
- 查看每种颜色的面积、置信度、长宽比、填充率和原因。
- 查看整批图片通过、漏检和多候选结果。
- 修改ROI、面积和置信度进行内存预览。
- 加载候选JSON。
- 保存候选JSON。

候选文件默认保存在：

```text
data\offline_vision_candidates
```

候选文件不会覆盖正式`camera.json`。

最新界面调整：

- 已删除大块HSV JSON文本框。
- HSV从正式配置或候选JSON读取。
- 可通过“编辑六色HSV范围”表格修改H/S/V上下限，同一颜色支持多个区间；确认后自动重新分析全部图片。
- 修改ROI、最小/最大面积或最低置信度时，旧结果立即作废；离开输入框或按回车后自动按新值重新分析整批图片。
- 图片按照预览区域等比例缩放。
- 原图、掩膜和标注图都能一次完整显示。
- 原因列已经改成中文。

中文原因包括：

- 面积低于最小值。
- 面积高于最大值。
- 轮廓尺寸过小。
- 长宽比不足（形状异常）。
- 形状置信度低于阈值。
- 未找到符合条件的该颜色目标。
- 存在多个相近候选目标。

用户提供的两张trays图片使用当前正式参数时，六种颜色均全部通过。

## 八、当前正式识别规则

2026-08-16已将正式blocks/trays识别改为与离线工具一致的`best_effort`规则；九点标定仍保持严格规则。

### blocks面积

```text
29,823 ≤ 面积 ≤ 108,834 px²
```

### trays面积

```text
22,642 ≤ 面积 ≤ 78,168 px²
```

### 形状

```text
短边/长边 ≥ 0.65
最小边长 ≥ 3 px
```

### 置信度

```text
confidence = 0.65 × 长宽比 + 0.35 × 矩形填充率
confidence ≥ 0.60
```

### 正式返回选择

先过滤小于`min_area_px × 0.05`的极小碎片，再按轮廓面积从大到小选择；面积相同才用置信度作为次级排序。面积、形状、低置信度和多候选均保留为诊断警告，不阻止返回。

### 正式装配漏检

- 只有完全没有可用HSV轮廓（`TARGET_NOT_FOUND`）时保持停稳并重拍一次。
- 第二次仍无目标则跳过当前组并继续处理后续组。
- 其他通信、相机、标定、坐标、机器人和IO错误仍立即停止。

## 九、已实施的正式宽松识别要求

正式装配现已实现：

- 面积异常不再拒绝。
- 形状异常不再拒绝。
- 低置信度不再拒绝。
- 多候选不再拒绝。
- 只要存在超过极小碎片下限的HSV轮廓，就选择面积最大的候选。
- 面积相同用置信度作为次级排序。
- 如果完全没有识别到该颜色，则跳过这一组。
- 继续处理下一组，保证1～6组流程都被处理。
- 九点标定规则保持严格，不作上述放宽。

实施方式：

1. 正式识别保留面积、形状、置信度计算，但只显示警告，不作为拒绝条件。
2. 多候选直接选择最大面积。
3. 完全没有HSV轮廓时，重拍一次；仍没有则标记跳过。
4. 托盘应在吸取方块前预先识别并缓存可用颜色，避免吸起方块后才发现目标托盘缺失。
5. 托盘识别允许部分颜色成功，缺失颜色单独记录。
6. 结果需要区分已处理任务数、成功装配数、跳过数和每组跳过原因。
7. 相机通信、标定、坐标换算、机器人运动、IO、急停等异常仍必须停止，不能作为普通漏检跳过。

## 十、当前验证状态

最新验证：

```text
111 passed
```

同时通过：

- MANIFEST文件完整性校验。
- 正式程序无硬件启动检查。
- 离线视觉工具启动检查。

当前没有完成：

- 真实blocks九点标定。
- 真实trays九点标定。
- 最新现场HSV整批复核。
- 接触Z和完整机械臂运动验收。

因此目前不能宣称已经完成真机比赛验收。

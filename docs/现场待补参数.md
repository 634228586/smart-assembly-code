# 现场待补参数

以下项目目前必须在界面中显示为红色或未配置：

赛前检查页中的红色行都可以点选；下方会用通俗中文说明当前缺项、合格标准、处理方法和操作位置。红色数量是真实缺项数量，不是界面显示错误。

- AUBO当前现场地址为`192.168.34.10:30004`、型号`AUBO-i5H`、控制器唯一机器人名称`rob1`、登录用户名`root`；电脑有线网卡使用`192.168.34.12/24`，不得与机械臂地址重复；
- `AUBO_RPC_PASSWORD`环境变量；
- TCP已固定为默认法兰`flange_zero_tcp`，六维offset全零，无需界面填写；实际回读改变时必须停止并重新确认；
- 负载质量、重心和工具安装人工核验已按现场决定从程序门控移除；
- 比赛待机、任务卡、方块、托盘四个关节点；
- Block红色抓取完整TCP和Tray红色放置完整TCP（各自包含真实接触Z）；
- 每场全局直达路径无人、无障碍物确认记录（程序无碰撞规划，不做逐路线验收）；
- 真机最大速度、关节/直线速度和加速度；
- MVS相机型号和唯一序列号；SDK/Runtime与包内服务文件已就绪；
- task_card、blocks、trays三套曝光、增益、白平衡、ROI及现场人工批准；
- 使用 Block/Tray 拍照取图后，离线复核 blocks、trays 两套 HSV、检测 ROI、面积和置信度，并直接更新 `config/real/camera.json`；不再执行六色同帧批准；
- blocks和trays真实九点标定；
- 光圈已确认使用DO0（False关、True开）；人工接线核验已按现场决定从程序门控移除，单步写入仍要求控制器输出回读一致；
- 吸盘已确认使用工具数字输出：TOOL_IO[1]长期True使能，TOOL_IO[0]为False吸取、True不吸；旧标准DO1映射作废。人工接线核验已从门控移除，正式写入仍要求回读一致；
- AI盒子已确认`192.168.34.200:8765`，`/health`返回200、`ready=true`、实际身份`arm_speech_service/m28-4-real`。本地兼容层会严格核对真实来源、ASR/唤醒词/VAD/Piper模型及TTS后端后规范化为内部协议v1和ASR/TTS能力；仍需完成一次真实麦克风ASR和扬声器TTS验收；
- Qwen模型、网络和密钥状态；
- 配置/代码/标定批准哈希已按现场决定从程序门控移除。

吸盘电平或 IO口改变时只改配置，不改 Python运动逻辑；修改后必须重新进行真实 IO测试和批准。

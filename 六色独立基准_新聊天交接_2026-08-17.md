# 六色独立基准与标定逻辑——新聊天交接文档

更新时间：2026-08-17

项目目录：`E:\iwen-codex\智能装配代码\装配赛正式代码`

## 1. 新聊天需要完成的任务

在现有真实机器人程序中实现“六色独立物理基准”逻辑：

- Block：红、橙、黄、绿、蓝、紫各保存一个抓取TCP，共6个。
- Tray：红、橙、黄、绿、蓝、紫各保存一个放置TCP，共6个。
- 总计保存12个TCP，其中6个抓取点、6个放置点；不是12个放置点，也不是做12次九点标定。
- Block和Tray仍各自只需要一套九点标定，共2套正式九点文件。
- 九点结束时已经会拍摄并识别一张六色参考图；运行时应使用“同颜色当前识别位置相对同颜色参考位置的微小变化”，叠加到该颜色独立示教的TCP上。

当前六色独立基准尚未实现。此前曾开始修改，但已完整回退；新聊天必须从当前正式代码状态重新审查后实施，不要假定存在未完成代码。

## 2. 当前正式运行状态

### 2.1 正式配置

- 机械臂地址：`192.168.34.10:30004`
- 正式配置目录：`config/real`
- 正式运动配置：`config/real/motion.json`
- Block正式标定：`calibration/real/blocks/9point_blocks.json`
- Tray正式标定：`calibration/real/trays/9point_trays.json`
- 实际/批准最高速度比例：70%
- 九点标定速度比例：15%
- Block九点步长：X=20 mm，Y=20 mm
- Tray九点步长：X=20 mm，Y=20 mm

### 2.2 当前正式九点标定

Block：

- calibration_id：`N9_BLOCKS_1B21E19FED8A`
- RMS：`0.050030438842198086 mm`
- 最大误差：`0.0929146959000011 mm`
- 步长：20 mm × 20 mm

Tray：

- calibration_id：`N9_TRAYS_2EEF291FD23D`
- RMS：`0.04602891065870678 mm`
- 最大误差：`0.06998265062243056 mm`
- 步长：20 mm × 20 mm

两套文件均已批准且是当前正式文件。文件中的 `approval_source` 仍为 `operator_skipped_direction_validation`，但操作者已经现场明确确认以下四项绝对正常：

- 工具坐标 +X 20 mm验证正常；
- 工具坐标 +Y 20 mm验证正常；
- 0°方向验证正常；
- +10°旋转验证正常。

后续诊断不要再把X/Y正负号、毫米/米单位或旋转正负号作为首要原因。

### 2.3 当前红色物理基准

当前 `motion.json` 仍是“每个场景仅一个红色基准”的旧结构：

Block红色基准抓取TCP：

```json
[
  0.4227064519636905,
  0.214059137883166,
  0.16799785098908923,
  -3.1402709564899376,
  -0.002645807968917888,
  1.5710672488776936
]
```

Tray红色基准放置TCP：

```json
[
  0.47393741887103474,
  -0.13109514129355312,
  0.17678781635956062,
  -3.140287999598938,
  -0.0026492721603778085,
  1.5710122692421091
]
```

这两个数值不是“无作用的配置”。它们负责把相机计算出的相对工具坐标偏移落到机械臂真实空间：

```text
实际目标TCP = 人工示教的物理基准TCP ⊕ 九点标定换算出的工具坐标偏移
```

其中 `⊕` 当前由机器人SDK的 `poseTrans` 完成。

## 3. 已确认的实际问题

现场最新结论不是“所有红色都准确”，而是：

- 红色方块可以准确抓取；
- 抓起后，放到红色托盘不准确。

因此当前首先暴露的是Tray放置侧问题，而不是Block抓取侧：

- Block红色抓取TCP和Block中心附近转换基本可信；
- Tray红色即使不需要远距离外推也放不准，应优先核查Tray红色放置TCP、Tray六色参考图中的红色中心，以及Tray目标叠加链路；
- 不要因为实现六色独立基准而掩盖红色Tray基准本身的问题。

最近一次已知Tray红色相对本色参考变化约为：

```text
ΔX = +1.453 mm
ΔY = +0.263 mm
```

如果实际红色放置误差明显大于约1～2 mm，优先怀疑Tray物理基准或参考对应关系，而不是这段小视觉修正。

另外，其他颜色曾出现明显误差。现有九点只覆盖中心附近约±20 mm，而部分颜色相对红色约60～75 mm，旧算法使用“红色基准 + 远距离相对红色偏移”，存在局部标定结果远距离外推的问题。六色独立物理基准的目的就是避免这种长距离外推。

## 4. 当前代码的实际计算链路

关键文件：

- `vision/workspace_localizer.py`
- `app/assembly.py`
- `app/robot_config_editor.py`
- `app/point_capture_worker.py`
- `app/ui.py`
- `config/real/motion.json`

### 4.1 视觉侧

`vision/workspace_localizer.py` 当前会：

1. 使用正式九点文件的 `homography_pixel_to_tool_mm`，把当前像素中心转换到工具平面毫米坐标；
2. 从同一正式标定文件读取 `reference_detections`；该字段已经包含红、橙、黄、绿、蓝、紫六色参考中心；
3. 同时输出两套位移：
   - `dx_tool_m`、`dy_tool_m`：当前目标相对“红色参考中心”的位移；
   - `delta_x_tool_m`、`delta_y_tool_m`：当前目标相对“本色参考中心”的位移；
   - `delta_r_rad`：当前目标相对本色参考角度的变化。

因此六色独立方案不需要重新发明视觉字段，优先复用现有的 `delta_x_tool_m`、`delta_y_tool_m`、`delta_r_rad`。

MVS服务在每次正式工作区识别请求中都会重新读取正式九点文件，并校验请求中的 `calibration_id`，不是一直缓存旧标定。

### 4.2 当前装配侧

`app/assembly.py` 当前仍执行：

```text
Block目标 = Block红色基准TCP ⊕ 当前颜色相对红色参考中心的dx/dy
Tray目标  = Tray红色基准TCP  ⊕ 当前颜色相对红色参考中心的dx/dy和角度修正
```

也就是说，当前虽然视觉已经知道六色各自的参考中心，但机械臂仍只有两个红色物理基准，尚未使用六色独立物理TCP。

## 5. 推荐实现方案

### 5.1 配置结构

把 `reference_anchors` 迁移为按场景和颜色保存，同时兼容或一次性迁移当前红色数据。建议结构：

```json
"reference_anchors": {
  "blocks": {
    "红": [6个TCP数值],
    "橙": "UNSET",
    "黄": "UNSET",
    "绿": "UNSET",
    "蓝": "UNSET",
    "紫": "UNSET"
  },
  "trays": {
    "红": [6个TCP数值],
    "橙": "UNSET",
    "黄": "UNSET",
    "绿": "UNSET",
    "蓝": "UNSET",
    "紫": "UNSET"
  }
}
```

迁移时保留当前两个红色TCP，不得静默丢失用户现有配置。未示教颜色必须明确为 `UNSET`，不能自动复制红色TCP冒充已示教。

### 5.2 UI和保存逻辑

在“机器人现场配置”页面增加颜色选择或12个清晰按钮：

- 保存当前TCP为 Block/红、橙、黄、绿、蓝、紫抓取基准；
- 保存当前TCP为 Tray/红、橙、黄、绿、蓝、紫放置基准；
- 每次保存只读取当前TCP，禁止自动运动；
- UI明确显示每个颜色是否已保存及完整6D TCP；
- 保存前继续执行机器人身份、活动TCP和只读状态核对；
- 比赛运行所选颜色缺少对应基准时，立即停止且禁止吸盘动作，错误信息必须指出具体场景和颜色。

### 5.3 新的运行公式

Block：

```text
block_anchor = Block[当前方块颜色]独立抓取TCP
block_target = block_anchor高位姿态
               ⊕ [delta_x_tool_m, delta_y_tool_m, 0, 0, 0, 0]
```

Tray：

```text
tray_anchor = Tray[目标托盘颜色]独立放置TCP
tray_target = tray_anchor高位姿态
              ⊕ [delta_x_tool_m, delta_y_tool_m, 0, 0, 0, delta_rz]
```

这里必须使用“相对本色参考”的 `delta_x_tool_m`、`delta_y_tool_m`，不再使用相对红色的 `dx_tool_m`、`dy_tool_m`做长距离位移。

Tray旋转仍需保持现有方块与托盘方向对齐逻辑，审查现有：

```text
delta_rz = normalize_square_angle(tray.delta_r_rad - block.delta_r_rad)
```

不要在没有真机证据时改变该旋转定义。

### 5.4 六色示教与参考图的正确顺序

每个场景推荐流程：

1. 完成该场景一次九点标定；
2. 九点结束时采集六色参考图；
3. 从参考图采集完成起，六个方块或托盘都不能移动；
4. 依次手动对准六种颜色并保存各自完整TCP；
5. Block和Tray各做一次，共保存12个TCP；
6. 以后目标发生小范围移动时，程序用同颜色的 `delta_x/y/r`做局部修正。

特别注意：独立颜色基准必须对应标定文件中同一张六色参考图里的同色目标。如果目标已经移动后才把当前位置保存为“参考基准”，运行时又叠加当前位置相对参考图的变化，会发生重复补偿。

### 5.5 标定失效与基准失效策略

新聊天实施时必须明确并测试：

- 更换活动TCP：Block/Tray全部颜色基准和两套九点标定均应失效；
- 更换某场景拍照点或重新生成该场景九点：至少该场景六个颜色的物理基准应失效或要求操作者重新确认；
- 不能像当前旧UI一样在批准新九点时无条件保留旧红色基准而不给出强提醒；
- 修改HSV不应自动改动机械臂TCP，但必须离线复验六色参考图与当前图的中心稳定性。

## 6. 验收标准

### 6.1 自动测试

至少补齐以下测试：

- 旧结构中两个红色TCP向新结构的兼容迁移；
- 12个颜色基准分别保存、读取和显示；
- 未示教指定颜色时拒绝运动；
- Block选择什么颜色，就只读取该颜色Block基准；
- Tray选择什么颜色，就只读取该颜色Tray基准；
- 新公式使用 `delta_x_tool_m/delta_y_tool_m`，不使用相对红色的 `dx_tool_m/dy_tool_m`；
- 红色行为在同色delta为零时退化为直接使用红色基准；
- 重新九点或改变拍照点后的基准失效规则；
- 配置字段校验、非有限数值、错误长度和 `UNSET`；
- 保留现有安全门控、身份校验、吸盘失败处理和停止逻辑。

完成修改后运行：

```powershell
.\.runtime\Scripts\python.exe -m pytest -q
```

如正式文件发生变化，还要按项目现有发布流程更新并验证清单，不能只改代码不验证完整性。

### 6.2 真机验收顺序

真机必须低风险、逐级验证：

1. 只读核对当前活动TCP、机器人身份、相机身份和两套calibration_id；
2. 先不吸取，仅显示六色各自计算出的局部ΔX/ΔY/ΔR；
3. 红色方块抓取到红色托盘，首先修正目前Tray红色放置不准的问题；
4. 单色逐一验证橙、黄、绿、蓝、紫；
5. 每个颜色先高位到点观察，再允许下降；
6. 六色全部通过后才允许完整自动流程。

## 7. 不要误改的内容

- 不要修改当前Block红色抓取基准来补偿Tray放置误差；Block红色已能抓准。
- 不要把12个TCP理解为12次九点标定。
- 不要重新拍HSV图片或重调HSV，除非用户在新聊天明确要求。
- 不要修改机器人IP、速度比例、任务卡、语音流程或发布门控；这些不属于本次六色逻辑任务。
- 不要用每色固定人工补偿量替代六色独立TCP加同色视觉微调。
- 不要自动运动去采集12个TCP；这些点必须由操作者现场示教并明确保存。

## 8. 新聊天可直接使用的启动指令

将本文件交给新聊天后，可以发送：

> 请读取 `E:\iwen-codex\智能装配代码\装配赛正式代码\六色独立基准_新聊天交接_2026-08-17.md`，审查当前正式代码后实现六色独立物理基准：Block六色抓取TCP、Tray六色放置TCP，共12个示教点；继续使用现有两套九点标定和六色reference_detections，运行时改为各颜色独立TCP加同色delta_x/delta_y/delta_r局部修正。保留当前两个红色TCP并做兼容迁移，补齐测试并运行完整测试。先不要连接或移动真实机器人。


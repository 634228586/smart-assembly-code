# 装配赛正式代码交接

## 2026-08-16后续流程确认（覆盖下方旧接触Z说明）

操作者分别保存Block六色抓取TCP和Tray六色放置TCP，共12个独立物理基准。当前旧结构的两个红色TCP已保留到新结构红色项，其他颜色明确为`UNSET`。每个TCP自身Z为该色接触高度。操作顺序为“完成场景九点及六色参考图 → 保持六色目标不动 → 逐色示教六个TCP”。运行时使用当前颜色独立TCP加本色`delta_x_tool_m/delta_y_tool_m/delta_r_rad`局部修正，不再使用相对红色的长距离`dx_tool_m/dy_tool_m`。重新九点、更换拍照点或活动TCP会使对应六色基准失效。

2026-08-16按用户最终确认重构正式视觉坐标逻辑：九点的九帧只跟踪同一个稳定红色目标，中心帧同时保存六色中心像素与角度；运行时以现场示教的Block红色基准抓取TCP、Tray红色基准放置TCP为物理锚点，计算每个当前本色目标相对标定红色中心的九点XY偏移。旋转使用托盘本色ΔR减方块本色ΔR。两个物理锚点已新增到“机器人现场配置”，当前`motion.json`均为`UNSET`，所以正式抓放会被赛前检查阻止，必须由操作者分别准确对准红色目标后只读保存。更改拍照点、TCP或重新批准九点会自动作废对应锚点。现有两套正式九点JSON已从各自中心采样原图补入六色视觉原点，无需仅为此重做九点。

2026-08-16正式blocks/trays已与离线工具统一为`best_effort`：过滤`min_area_px × 0.05`以下极小碎片后按面积最大返回，面积相同以置信度打破平局；面积、形状、低置信度和多候选只记警告。托盘在抓取前预识别并允许部分颜色返回；某目标连续两帧无轮廓时跳过该组并继续。通信、相机、标定、坐标、机器人、IO及急停异常仍立即停止，九点标定保持严格唯一方形候选。

更新时间：2026-08-17。

2026-08-17按用户现场要求，将正式比赛、维护“移动到此点”以及九点进入/返回中心的实际速度比例和批准上限统一由40%调整为70%；后续又将九点九个采样点之间的速度由15%调整为50%。

2026-08-17用户将机械臂RPC地址由`192.168.34.10:30004`调整为`192.168.34.203:30004`。正式`endpoints.json`、机器人稳定绑定身份以及两套当前正式九点中的机器人绑定已同步为`AUBO-i5H|192.168.34.203|rob1`；端口号仍为`30004`。旧地址只保留在历史验收记录中，不代表当前端点。

2026-08-17用户随后将机械臂RPC地址恢复为`192.168.34.10:30004`。当前正式`endpoints.json`、机器人稳定绑定身份以及两套正式九点中的机器人绑定均已恢复为`AUBO-i5H|192.168.34.10|rob1`；`.203`仅保留在上述变更历史中，不参与当前运行。

后续重新审查 HSV 时，Block 和 Tray 分别使用“拍照并用当前参数识别”或“拍照取图”产生的图片，按时间各自选择最新的一张 Block 和 Tray 原图；不使用标注图、九点图片或任务卡图片。

用户明确要求开始 HSV 调整后，最终计算值必须直接写入正式 `config/real/camera.json` 中 blocks/trays 各自的 `detector.hsv_ranges`，不能只保存离线候选JSON。写入后用同两张原图离线复验、更新MANIFEST并运行测试；真实运行前重启MVS视觉服务加载新配置。未另行要求时不改曝光、增益、白平衡、ROI、面积或置信度。

## 2026-08-13 当前权威快照（下一聊天从这里继续）

本节是当前代码与配置的最新状态；如与下方按日期保留的历史描述冲突，以本节和当前文件为准。当前版本为 `0.4.15-detection-visual-evidence`，状态仍是 `hardware_validation_required`，`robot_motion_authorized=false`。尚未完成真实相机采集闭环、真实九点和完整比赛运动验收，禁止宣称正式版已经完成。

2026-08-14新增颜色识别可视化证据：相机与视觉页、九点页和自动流程监控页固定显示最近一次带框结果。每次blocks/trays六色验证、九点单色检测和正式工作区定位均在判断成功/失败前保存原图与`-annotated.png`；绿色框为最终选中目标，黄色为造成候选冲突的合格候选，红色为因面积、方形度或置信度被过滤的候选，青色框为ROI。失败响应也携带两张路径和逐色候选摘要，因此未找到、候选不唯一或配置异常时仍能查看本次实拍证据。原始图不覆盖，协议同时验证成功结果的两张证据文件存在。

2026-08-14按用户现场要求，所有真实正式/维护`MoveJoint`和`MoveLine`入口，以及正式流程吸盘入口，在收到本次明确操作后若发现`RuntimeMachine=Stopped`，都会先通过机器人身份、RobotMode Running、SafetyMode Normal、exec_id=-1和零TCP offset门控，再自动调用`RuntimeMachine.start()`并等待状态确认为Running，然后继续本次指令。绝不调用`runProgram()`；start失败、超时、执行队列非空或安全状态异常时不下发运动。九点流程原有显式start保留，后续入口重复检查为无副作用。

2026-08-15新增独立离线视觉工具：`启动离线视觉工具.cmd`。可批量导入图片/文件夹，按blocks/trays正式检测参数查看原图、六色HSV综合色掩膜、标注结果、逐图六色明细和整批通过率；可在内存中修改ROI、面积、置信度和六色HSV后重新分析。保存只写`data/offline_vision_candidates`候选JSON并记录来源图片，明确不修改正式`camera.json`、不连接相机/机械臂。用户给出的两张trays样图用当前正式参数均为六色全部通过。

2026-08-14“直接到所选拍照点并生成/重新生成九点文件”改为始终启动本次最新会话：机器人移动前，总控读取MVS健康状态并取消旧的未完成九点；服务端`calibration_begin`也会防御性替换残留会话并清除同场景旧候选内存。新九点从第1点重新采集，不混用旧样本。旧正式批准文件仍保留到新候选完成方向验证并批准，届时按原流程归档旧正式文件。

2026-08-14九点页的下方预期坐标表已与上方当前所选场景X/Y步长实时联动；例如10/10会立即生成±10网格。输入暂时无效时清空下表，不再保留旧20/15数据；预期坐标表设为只读，保存、重新加载和场景切换都会保持一致。

2026-08-14按用户现场决定，取消三套MVS参数的预先回读和运行时回读一致性阻断。三套已保存参数由操作者人工批准，`camera.json`中task_card/blocks/trays均为`approved=true`；运行时只调用SDK写入保存值，不读取Exposure/Gain/BalanceRatio/ROI节点做一致性校验。写入、软件触发、取图及实际新帧尺寸异常仍会明确失败停止。

2026-08-14将已有MVS当前参数只读按钮移到三套参数表正上方，标记为“从当前 MVS 读取并填入上表”。该操作仍只读曝光、增益、白平衡和ROI，并复制到三行；不采图、不保存、不批准。MVS驱动同时兼容不导出`MV_CC_Initialize/Finalize`的旧版Runtime。当前全套63项无硬件测试通过，MANIFEST已更新并验证。

2026-08-14按用户要求，九点标定改为单色模式：不要求blocks/trays六色检测已批准，也不依赖六色面积阈值；每帧仍强制所选颜色只有一个有效方形候选，否则立即失败停止。2026-08-15进一步将面积/HSV改为“Block/Tray取图后离线分析并直接写入camera.json”，界面不再提供手工编辑和六色同帧批准入口。三套MVS参数已经操作者人工批准，可用于标定采集。

2026-08-14九点标定进一步明确为维护运动：允许控制器`RuntimeMachine=Stopped`，不要求启动ARCS当前加载程序；标定中的中心MoveJoint和九点MoveLine改用维护运动门控。RobotMode Running、SafetyMode Normal、exec_id=-1、零TCP offset、逐点确认和停止监控仍强制。2026-08-15按现场要求，中心MoveJoint改为40%，仅九个采样点之间的MoveLine保持15%。

### 设备更换后的强制重新检查（长期规则）

用户要求把下列规则作为后续聊天和现场工作的持久约束：只要更换了真实机械臂、语音盒子、MVS相机，或修改了大模型API Key/Base URL/模型名，就必须把对应设备视为“新设备”，重新做一次真实连接、身份、协议和功能验收；不得因为旧设备曾经通过就直接沿用。检查可以自动化，但任何真实运动、录音、播报、相机触发或云端调用前仍须明确告知用户。

- 更换机械臂或控制器：重新核对电脑网卡与机器人IP、RPC端口、唯一`rob1`身份/型号、SDK登录、RobotMode/SafetyMode/Runtime/exec_id、默认法兰TCP六项全零；重新示教四个固定关节点和pick/place接触Z；重新核对TOOL_IO吸盘与DO0光圈；低速逐点和急停验证。由于机器人、拍照姿态或眼在手几何关系改变，blocks/trays两套真实九点必须重新做并批准，旧标定不得复用。
- 更换语音盒子或重装/升级盒子服务：重新核对IP、端口8765和网络连通；读取真实`/health`并核对服务身份、版本、真实模型和TTS后端；该自定义服务不提供`/openapi.json`，不得把404误判为业务路由缺失；实测首次“小具同学”唤醒、后续免唤醒、麦克风ASR和扬声器TTS。完成一次适配验收后，比赛当天原则上只需开机、接网并启动橙色`SpeechRelease语音服务`，保持终端窗口打开。
- 更换MVS相机、镜头、安装位置、分辨率或工作距离：重新读取序列号和三套真实采集参数，重新采集blocks/trays现场图片并离线计算面积/HSV，再完成两套九点；旧相机参数和标定不得复制使用。
- 更换大模型API Key、Base URL或模型名：重新检查三个Windows用户环境变量，并做一次不泄露密钥的最小真实API调用；随后使用真实任务卡图片验证严格JSON结果。API Key不得写入项目或交接文件。

### 当前已经写入的真实信息

- 正式目录固定为 `E:\iwen-codex\装配赛正式代码\装配赛正式代码`，不得回到旧 `QT MAKING`、`QT界面`或 `qt_learning`恢复仿真数据。
- 机器人当前端点为 `192.168.34.10:30004`；比赛电脑有线网卡为`192.168.34.12/24`，AI盒子为`192.168.34.200`。厂家/型号 `AUBO/AUBO-i5H`，控制器唯一名称 `rob1`，稳定绑定身份为 `AUBO-i5H|192.168.34.10|rob1`。登录用户名 `root`，密码只通过 Windows 用户环境变量 `AUBO_RPC_PASSWORD`读取，禁止写进项目。2026-08-14曾在该地址完成只读登录；当前运行前仍须重新核对实时身份与状态。
- 真实只读连接曾成功：SDK/Server接口版本均为0.24.0，当时状态为 RobotMode Running、SafetyMode Normal、exec_id=-1、RuntimeMachine Stopped、TCP offset六项全零；只读后已断开，没有借此宣称当前仍保持相同状态。
- 活动TCP固定为 `flange_zero_tcp`，offset六项全零；运行时仍会回读，出现任何非零偏移就拒绝继续。
- 四个关节点已从真实控制器读入 `motion.json`：`competition_standby`、`task_card_photo`、`blocks_photo`、`trays_photo`均已有六个弧度值，不再属于待填项。它们尚未完成整套真实运动验收。
- 吸取/放置接触Z目前仍是候选值 `0.1 m / 0.1 m`，尚未按真实工件接触面重新示教确认。界面已有两个独立“只读当前TCP Z并保存”按钮。
- 当前运动参数：九点九个采样点之间保持50%；完整比赛、维护“移动到此点”以及九点进入/返回中心均为70%，批准上限为70%；关节速度0.15 rad/s、关节加速度0.30 rad/s²；直线速度0.03 m/s、直线加速度0.10 m/s²。九点稳定等待1秒，当前blocks/trays的X/Y步长均为10 mm。
- MVS相机序列号已经写入 `DA3968689`，安装方式眼在手；机器级MVS Runtime及包内wrapper检查通过。
- AI语音盒子端点为 `192.168.34.200:8765`。真实`/health`返回HTTP 200、`ready=true`、`arm_speech_service/m28-4-real/provider=real`，ASR、唤醒词、VAD和Piper模型存在，TTS后端包含Piper与Edge。该服务不提供`/openapi.json`且业务路由只接受POST；本地已增加严格`arm_speech_m28_v1`兼容层。2026-08-14已实测Piper中文播报有声，实时麦克风ASR返回中文文字并报告`live_capture/azure_keyword`；按用户“检测到文字即可”的标准通过。客户端同时新增`tts_skipped`和实际播报文字一致性校验，禁止把仅响提示音或跳过合成误判为成功。
- 云端任务卡视觉所需 `DASHSCOPE_API_KEY`、`DASHSCOPE_BASE_URL`、`DASHSCOPE_MODEL`在最近静态检查中均存在。不得把密钥内容写入项目或交接。

### 用户最后确认的IO映射

- 标准数字输出 `DO0`控制光圈：True开、False关。用户已现场操作确认可用；按用户要求，光圈现场测试标志不再作为比赛授权阻断项。
- 工具数字输出 `TOOL_IO[1]`是吸盘长期使能，比赛吸盘工作期间保持1。
- 工具数字输出 `TOOL_IO[0]`是吸取控制：0吸、1不吸。正式代码和IO单步页均先保证`TOOL_IO[1]=1`，再切换`TOOL_IO[0]`；释放时不关闭使能。
- 吸盘现场测试标志也已按用户要求取消授权阻断，但IO写入仍保留真实机器人状态门控、逐次确认和写后回读。禁止恢复旧“标准DO1是吸盘”的映射。

### 用户明确取消的赛前阻断

以下项目已从赛前红项及运行授权阻断中移除：负载质量与重心、工具安装人工核验、真机运动总放行、配置/代码/标定批准哈希、重复的robot/camera/baseline汇总红项、吸盘`real_wiring_verified`、光圈`real_wiring_verified`、MVS参数预先回读和运行时回读一致性。兼容字段可能仍保留在JSON中，但不能重新作为授权条件。实时机器人身份与状态、相机写入/触发/取图、两套九点以及非目标漏检类异常停止仍然有效；颜色参数完整性检查不再依赖`detector.approved`。

### 当前真正剩余的静态赛前失败

三套MVS参数已填写并人工批准，不再属于静态失败。当前仍需处理：

1. blocks颜色检测需根据当前现场实拍图离线确认HSV和面积范围。
2. trays颜色检测需根据当前现场实拍图离线确认HSV和面积范围。
3. `calibration/real/blocks`和`calibration/real/trays`目前都没有已批准九点JSON；必须分别完成九点和方向验证。

除此之外，真实比赛前仍要完成两个实际操作但当前不会显示为上述四项静态红项：把pick/place接触Z从0.1 m候选值改为真实示教值；完成AI盒子协议适配与ASR/TTS验收。四个关节点虽然已经保存，也必须由操作者确认确实对应目标位置。

### 拍照保存位置和“本次新帧”逻辑

- 所有运行证据根目录：`data/sessions/`。正式比赛会话名形如 `competition-YYYYMMDD-HHMMSS-ffffff`。
- 任务卡：`data/sessions/<session_id>/task_card/<request_id>.png`。
- 方块：`data/sessions/<session_id>/blocks/<request_id>.png`。
- 托盘：`data/sessions/<session_id>/trays/<request_id>.png`。
- profile验证、面积估算、六色验证分别保存到同一session下带`-profile-validation`、`-detector-area-estimate`、`-detector-validation`的场景目录。
- 九点原图和标注图：`data/sessions/<nine-session>/calibration/<blocks|trays>/`，包括`precheck.png`、`point-1.png`至`point-9.png`以及对应`-annotated.png`；同一步骤存在时拒绝覆盖。
- 截至2026-08-13静态检查，`data/sessions`内实拍图片数量为0，说明正式目录尚未留下真实采集证据。
- MVS采集每次停止旧取流、按保存值写入对应profile、重新开始取流、执行`TriggerSoftware`，然后取一帧；不读取参数节点做一致性校验。每个请求使用唯一`request_id`，同名文件拒绝覆盖。
- 任务卡识别器直接接收本次响应的精确`image_path`，不会扫描目录挑“最新文件”。方块/托盘则直接对本次触发得到的`capture.image_bgr`做OpenCV定位，同时把同一帧保存；也不会读取旧图片。
- 客户端会检查请求号、session、相机序列号、profile、文件确实属于本场session、拍摄时间不得早于请求开始时间及最大新鲜度。识别失败必须明确失败且不继续运动。

### 2026-08-13视觉协议问题已修复

静态审查曾发现服务端与客户端的“允许字段集合”不一致：

- `RealMvsVisionService._capture_task_card()`实际返回`frame_number`、`image_width`、`image_height`、`parameter_readback`，但`RealVisionClient.capture_task_card()`使用严格`set(result)==required`，其required中没有这些字段。因此真实任务卡拍照可能已经保存成功，却被客户端报“字段不完整或包含未知字段”。
- `RealMvsVisionService._capture_and_locate()`同样返回`frame_number`、`image_width`、`image_height`、`parameter_readback`，但`validate_workspace_result()`严格要求只有基础字段加检测结果。因此blocks/trays真实定位结果也可能被拒绝。
- 当前协议使用帧号、图像尺寸和`configured_parameters`记录本次写入值；不再要求`parameter_readback`。其他未知字段仍会被拒绝。
- 已新增“真实MVS服务完整响应直接送入真实客户端”的三条无硬件集成测试，覆盖任务卡、blocks和trays。2026-08-13全套55项无硬件测试通过，MANIFEST已更新并验证。
- 该修复只证明服务端与总控的代码协议链路已统一，不等于真实相机采集和比赛识别已经现场打通；后者仍须按下列顺序完成硬件验收。

### 下一聊天的准确工作顺序

1. 三套MVS参数已经保存并人工批准；无需执行参数回读。启动MVS服务后，运行时会按保存值写入并获取新帧。
2. 分别点击`Block 拍照取图`和`Tray 拍照取图`采集当前现场图片，离线计算并写入面积/HSV，随后重启MVS服务。
3. 修正pick/place接触Z：用户在ARCS手动示教到真实接触高度，分别点击只读保存按钮；此步骤不运动但会连接机器人读取TCP，仍需现场确认。
4. 分别完成blocks、trays真实九点：进入/返回中心使用70%，九个采样点之间保持50%，逐点双确认；任何失败/停止/重启均废弃整套并从第1点重来；通过最大残差≤0.5 mm、RMS≤0.3 mm以及+X10 mm、+Y10 mm、0°、+10°验证后批准。
5. 对AI盒子完成真实ASR/TTS功能验收。第一次监听说“小具同学”加任务触发指令；同一场后续指令不再要求唤醒词。健康身份适配已完成，但真实录音和播报未验收前不能假成功。
6. 最后才做真实单点、下降、吸取、单组、六组及双任务卡逐级验收。任何真实机器人、相机或IO操作前必须明确说明连接对象、读取内容或动作内容，并等待用户现场确认。

2026-08-14按用户要求，“机器人现场配置”的四个固定关节点新增逐行“移动到此点”按钮。按钮使用已保存正式点位和独立维护速度配置（当前40%），弹窗明确目标六轴、速度及无碰撞规划风险；确认后使用维护专用`AuboRealGateway.move_joint_maintenance_and_wait`，校验唯一机器人、RobotMode/SafetyMode、空执行队列和零TCP offset，持续等待真实到位。点位移动与比赛、九点、相机、IO及其他维护Worker互斥；关闭窗口会先请求停止运动。

2026-08-14现场发现直接SDK单点`MoveJoint`在`RuntimeMachine=Stopped`时被过严门控拒绝。依据AUBO官方定义，RuntimeMachine管理脚本解释器，不是直接运动接口的上电状态。现新增独立`move_joint_maintenance_and_wait`：只供“机器人现场配置→移动到此点”使用，允许Runtime Stopped，但仍强制RobotMode Running、SafetyMode Normal、exec_id=-1、零TCP offset、速度上限、停止请求、运动中安全监控和最终误差；完整比赛、九点标定、直线运动及吸盘流程继续使用原完整Runtime门控。随后按用户现场要求，将维护单点速度/上限和完整比赛通用速度/上限均设为40%；2026-08-15进一步将九点中心运动设为40%，仅九个采样点之间保持15%。

2026-08-14用户现场纠正吸盘控制电平：`TOOL_IO[1]`是长期使能，必须保持1；`TOOL_IO[0]`为吸取控制，0吸、1不吸。旧“标准DO1控制吸盘”作废。正式执行和IO单步页均使用SDK`setToolDigitalOutput/getToolDigitalOutput`，若工具口仍为输入则先用`setToolIoInput(index, false)`切为输出并核对；每次先确保TOOL_IO[1]=1，再写TOOL_IO[0]，释放时不关闭TOOL_IO[1]。标准DO0光圈映射保持。

2026-08-12新增吸取/放置接触Z各自的只读采集保存按钮。用户先在ARCS中示教当前TCP到真实接触高度，逐次确认后Worker按唯一`rob1`只读当前TCP位姿并保存`tcp_pose[2]`（Base Z）到选中的pick/place；不运动、不上电/startup、不写IO，不要求其他红项完成。与点位/IO/相机/九点Worker互斥。运行时`AssemblyExecutor._with_z`确实直接使用该Base Z。

2026-08-12曾新增blocks/trays“真实拍照并自动填写面积”及六色批准流程；2026-08-15按当前操作方式从用户界面移除，改为Block/Tray分别取图后离线分析并直接更新`camera.json`。

2026-08-12用户进一步取消光圈`real_wiring_verified`赛前检查及授权阻断。DO0映射保持True开、False关；IO单步按钮、实时机器人门控、写入返回和输出回读仍保留，字段兼容保留为false。

2026-08-12用户进一步取消吸盘`real_wiring_verified`赛前检查及授权阻断；该字段兼容保留为false。最新实际映射见上方TOOL_IO说明。

2026-08-12用户明确取消以下赛前检查及运行阻断：负载质量与重心、工具安装人工核验、真机运动总放行、配置/代码/标定批准哈希；同时删除robot/camera/baseline三个重复通用汇总红项。运行构建器中的批准基线强制比较同步移除，避免只隐藏界面却继续阻断。`baseline.json`、payload/tool_mounting/real_robot_verified旧字段为兼容保留但不再读取为授权条件。运动Worker的短时配置指纹仍保留，用于发现同一次真实操作期间配置发生变化。2026-08-15颜色参数改为实拍图离线计算，两套九点、机器人唯一身份、实时RobotMode/SafetyMode/exec_id/TCP检查及识别失败停止继续强制执行。

2026-08-12新增“只读当前MVS参数并填入三套”：明确确认后独占连接`DA3968689`，只读曝光、增益、白平衡R/G/B、Width/Height/Offset并复制到三套候选表格；不采集、不触发、不修改参数数值、不保存、不批准。机器人控制器不提供可靠厂家序列号，因此不再要求伪填。2026-08-14依据控制器网络页纠正标定身份绑定为`AUBO-i5H|192.168.34.9|rob1`；真实连接严格要求端点`192.168.34.9`返回唯一名称`rob1`。

2026-08-12按用户现场操作要求，blocks/trays颜色检测 ROI改为自动绑定各自采集画面的完整范围`[0, 0, Width, Height]`。界面四个坐标只读显示，无需手填。2026-08-15起，颜色参数改由 Block/Tray 实拍图离线计算后直接更新`camera.json`，不再执行检测批准。相机序列号已静态写入`DA3968689`，当前现场图仍需重新采集复核。

2026-08-12用户决定TCP固定使用控制器当前默认法兰值：内部标识`flange_zero_tcp`、offset六项全零。版本0.4.4起机器人现场配置页移除TCP名称、六维表格和单位控件，不再要求填写；九点/识别证据继续用固定标识绑定。每次运动仍回读实际offset，任何非零变化都会拒绝，不能静默继续。

2026-08-12按用户要求写入首轮保守调试运动参数：最大授权比例15%、默认比赛比例5%、关节速度0.15 rad/s、关节加速度0.30 rad/s²、直线速度0.03 m/s、直线加速度0.10 m/s²；九点直线速度0.03 m/s、加速度0.10 m/s²、稳定等待1秒。尚未真机运动验证，现场可根据实际快慢再修改。九点开始前的一次全局无人/无障碍物确认保留。

2026-08-12新增“IO单步调试”页；最初吸盘曾按标准DO1配置，后已由上述TOOL_IO现场证据纠正，禁止恢复旧映射。光圈按钮先回读标准DO0再切换并再次回读。所有真实IO操作均使用独占Worker、维护IO门控和现场确认弹窗，不自动上电/startup、不运动。

2026-08-12按用户现场调试需求将手动IO从比赛前置条件中解耦：IO单步页不要求相机、九点、点位、TCP、RuntimeMachine或批准哈希，只保留唯一机器人身份、RobotMode Running、SafetyMode Normal和exec_id=-1检查及逐次确认/回读。正式运动门控和赛前17项检查未删除，比赛流程中的吸盘仍使用完整运动门控。

2026-08-12曾把AUBO网关`192.168.34.1`误记为控制器IP，随后又曾误记为`.9`。2026-08-14由现场用户最终确认并实测：真实AUBO IP为`192.168.34.10`，比赛电脑为`192.168.34.12/24`，AI盒子为`192.168.34.200`；AUBO RPC TCP `30004`可达。

随后经用户授权对AI盒子做只读TCP端口探测，开放端口为22/111/3389/5555/8765；`http://192.168.34.200:8765/health`返回200、`ready=true`、实际`service=arm_speech_service`、`version=m28-4-real`，并显示ASR/唤醒词/Piper模型存在。2026-08-14已按真实健康字段实现严格兼容层并确认`POST /v1/asr`、`POST /v1/tts`存在；仍需真实ASR/TTS功能验收。

用户随后明确确认真实RPC凭据并授权只读登录。2026-08-14 AUBO SDK连接最终地址`192.168.34.10:30004`成功，SDK/Server接口版本均为0.24.0，控制器只返回唯一名称`rob1`；型号由铭牌确认`AUBO-i5H`，用户名为`root`，密码没有写入文件。最新正式Gateway只读结果为RobotMode Running、SafetyMode Normal、exec_id=-1、RuntimeMachine Stopped、TCP offset全零，随后立即断开。没有上电/startup、启动Runtime、运动或写IO。

用户级`AUBO_RPC_PASSWORD`已在当前Windows账户设置并只验证存在性，未打印或写入项目JSON。正式启动器在进程变量缺失时会从Windows用户级环境读取，解决旧Explorer环境导致IO页报告凭据未配置的问题；仍不记录密码内容。

## 权威范围和阅读顺序

本文件是当前正式真实机械臂工程的最高优先级交接。旧文件 `E:\iwen-codex\QT MAKING\HANDOFF.md`只用于追溯仿真开发历史；如有冲突，以本文件和正式目录当前代码为准。

下一个聊天应依次阅读：本文件、`README.md`、`docs/比赛启动手册.md`、`docs/真机示教与标定手册.md`、`docs/现场待补参数.md`。可直接复制根目录的`下次聊天交接文本.txt`作为新聊天首条消息。

## 当前状态

已完成里程碑 A 总控软件闭环，并已新增包内 `real_mvs_vision` 服务：按唯一序列号打开相机、按保存值写入三套 profile、软件触发、任务卡实拍保存、blocks/trays颜色定位、真实九点矩阵换算、拍照点回传核验和明确失败响应。按用户决定不读取MVS参数节点做一致性校验。厂商 Python wrapper已随包保存，机器级 Runtime仍由 MVS安装程序提供。

“机器人现场配置”页已从说明占位页升级为可编辑表格：活动 TCP名称和六维 offset、比赛待机/任务卡/方块/托盘四个关节点、吸取/放置接触 Z。支持 ARCS常见 `mm/deg`和配置存储 `m/rad`转换。整体保存采用校验和原子替换，只写 `robot.json`/`motion.json`，并撤销真机总放行；不提供新增/删除点位。另有“读取当前机械臂关节角并直接保存到选中点位”维护按钮：逐次确认后按唯一`rob1`只读J1～J6并原子保存一个点位，不运动、不要求其他点/TCP/接触Z/视觉/基线完成，改变blocks/trays拍照点会撤销对应旧九点批准。

“相机与视觉”页提供唯一 MVS序列号，以及 task_card/blocks/trays三套曝光、增益、白平衡和图像 ROI。现场操作者勾选确认后可保存并立即人工批准，不枚举、打开或触发相机。运行时只按保存值写入，不进行参数回读一致性检查。拍照点/TCP来自机器人配置，标定 ID由真实九点标定生成。

“维护与真实九点标定”页已实现 blocks/trays两套独立标定：固定目标、眼在手、当前活动 TCP、工具 X/Y蛇形九点、中心运动70%、九个采样点之间50%、首次逐点双确认、真实新帧证据、最大点误差 0.5 mm/RMS 0.3 mm以及 +X/+Y/0°/+10°四项验证。批准时旧正式文件归档，最新文件原子写入 `calibration/real/blocks/9point_blocks.json`或`calibration/real/trays/9point_trays.json`。未连接硬件验证。

同页新增显式机械臂控制条件指示：用户在 ARCS手动上电/startup后点击只读检查，Worker核对唯一机器人身份、RobotMode、SafetyMode、exec_id、RuntimeMachine和活动 TCP后立即断开；绿色仅表示当前控制门槛通过，红色显示原因。检查不运动、不上电、不startup、不写IO，正式标定开始时仍重复门控。

“赛前检查”页已经改为现场人员可读的中文检查名称。主表显示通俗检查内容、当前情况、合格标准、通过/未通过、处理位置和是否阻止比赛；表格支持横向滚动及单元格全文提示。选中任意行后，下方显示“检查什么、当前缺什么、怎么处理、去哪里处理”，最后才保留技术字段。红色数量代表真实缺项，不是显示故障。

尚未完成里程碑 B：当前电脑已安装 MVS客户端 5.0.2和 SDK Runtime 4.8.1.2；机器人端点、稳定身份、固定零TCP、四个关节点、三套MVS参数、相机序列号、AI盒子IP/端口及IO映射已经写入。当前仍需用现场 Block/Tray 实拍图复核颜色参数，并完成两套九点、真实接触Z以及完整真机运动验收。因此比赛授权必须保持禁用，不能宣称真机已验收。

## 用户确认的最终设计

- 正式运行目录：`E:\iwen-codex\装配赛正式代码\装配赛正式代码`。
- 只在当前电脑运行，但包内包含专用 `.runtime`。
- 一台安装在机械臂末端的 MVS相机承担任务卡、方块和托盘拍摄。
- 任务卡不做九点标定；blocks和trays分别做真实九点。
- 只保留一个方块拍照点和一个托盘拍照点。
- 拍照点之间直接 `MoveJoint`，不增加过渡点，也不做逐路线验收。每场授权前由操作者一次性确认全部直达路径无人且无障碍物；程序没有碰撞规划。
- 首次监听需要“小具同学”；之后不需再次唤醒。
- 指令匹配继续采用短语任意单字命中。
- 任务一卡只播报/记录；任务卡二合法后开始六组装夹。
- TTS失败不阻断数据和硬件门控均有效的任务二执行。
- 托盘在抓取前拍摄并缓存可用颜色；方块每组重新拍摄；仅目标不存在时原地重拍一次，仍不存在则跳组。
- 六组完成后返回任务卡拍照点，返航成功后才确认任务二完成。
- 吸盘IO已按现场确认确定为TOOL_IO[1]长期使能、TOOL_IO[0]的0吸/1放；现场改变后必须同步修改并重新验证。
- 任何人工停止、急停、硬件断线、配置修改或重启都会撤销比赛授权，不自动续跑。

## 正式边界

- 正式模块不得导入旧 `qt_learning`数字步骤。
- 正式代码只读 `config/real`和 `calibration/real`。
- 不得使用仿真点位、测试图片、测试标定或开发发送器。
- 端口集中在 `endpoints.json`，连接后必须验证服务身份。
- 机器人连接必须匹配配置中的唯一机器人名称，不能盲选第一个。
- 每条运动前重新检查 RobotMode、SafetyMode、exec_id和 TCP offset。
- Qt/AUBO操作仍需放在独占 Robot Worker线程；当前 `AssemblyExecutor`是同步核心，接入 Qt时不得在 GUI线程运行。

## 最近验证

包内运行环境导入通过：PySide6 6.11.1、OpenCV 4.12.0、NumPy 2.2.6、OpenAI 2.50.0、HTTPX 0.28.1、pyaubo-sdk 0.24.1；`pip check`通过。

2026-08-13全套无硬件测试已扩展至55项并通过；其中新增三条测试直接把真实MVS服务的完整响应交给真实客户端严格schema，分别覆盖task_card、blocks和trays。MANIFEST已重新生成并验证。

## 下一步

1. 用户在ARCS手动输入PIN、上电并startup；所有实际连接或动作仍需逐次现场确认。
2. 由用户明确启动 `启动MVS视觉服务.cmd`，按唯一序列号使用已保存参数，分别完成Block/Tray取图并离线更新面积与HSV。
3. 重新示教并只读保存pick/place真实接触Z；复核四个已保存关节点。
4. 分别完成blocks和trays九点及四项方向验证。
5. 对已完成健康身份适配的AI盒子执行一次真实ASR/TTS功能验收；禁止把健康通过冒充为录音、识别和播报均已通过。
6. 按文档从只读身份、低速单点、无IO下降、IO单测、单组、六组到双任务卡逐级验收。
7. 每次代码修改后生成`MANIFEST.sha256`并跑全套无硬件测试；最终仍保持`hardware_validation_required`，直到真实验收全部完成。

在上述步骤完成前，不得把版本状态从 `hardware_validation_required`改为比赛正式版。
